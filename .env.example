# 复制为 .env 并填入自己的值(.env 不会被提交); 远程 CI 用 GitHub Secrets。
# --- OpenAI 系 API(必填) ---
OPENAI_API_KEY=
# 走中转/网关时填其地址(官方 API 留空)
OPENAI_BASE_URL=
# --- 抽取模型(可选), 默认 gpt-5.6-sol, 低成本可换 gpt-5.6-terra / gpt-5.6-luna ---
OPENAI_MODEL=
