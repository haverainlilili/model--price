# 复制为 .env 并填入自己的值(.env 不会被提交); 远程 CI 用 GitHub Secrets。
# --- Claude 结构化抽取(必填一项) ---
ANTHROPIC_API_KEY=
# 走第三方中转/网关时填其地址(官方 API 留空)
ANTHROPIC_BASE_URL=
# ANTHROPIC_AUTH_TOKEN=
# --- 抽取模型(可选), 默认 claude-opus-5, 低成本可换 claude-sonnet-5 ---
CLAUDE_MODEL=
